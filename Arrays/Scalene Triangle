#include <stdio.h>

int main() {
    int t;
    scanf("%d", &t);

    while (t--) {
        int a, b, c;
        scanf("%d %d %d", &a, &b, &c);
        // Your code goes here
        if(a!=b && b!=c && c!=a && a!=c && b!=a){
            printf("YES\n");
        }
        else{
            printf("NO\n");
        }
    }
}
